# Curator Report — 2026-08-28 14:19 UTC (model: opencode/hy3-free)
> Auto-generated by opencode headless (free-model traversal + kitesurf) — experimental, needs human review.

## Summary
General health audit (no PR/Issue event). Bilingual READMEs are perfectly in sync (identical 95-repo sets, 96 table rows each) and no duplicate entries were found. However, **star counts are severely stale** — every sampled repo shows 8%–339% growth versus the listed "as of 2026-08-15" values, indicating the list needs a full star refresh. All links resolve (npm 403 was bot protection; registry confirms both packages exist) and sampled plugins correctly carry the `dsh-plugin` topic.

## Preliminary Checks
No `GH_PR` / `GH_ISSUE` set → preliminary PR/Issue checks N/A. Repo-level health audit performed instead (see Repo Health).

| Check | Result |
| --- | --- |
| Bilingual sync (EN vs ZH repo sets) | ✅ identical 95 repos, 96 rows each |
| Duplicate `owner/repo` entries | ✅ none |
| Star drift sample | ❌ severe (see Repo Health) |
| Link health (HTTP 200) | ✅ all resolve |
| `dsh-plugin` topic compliance (sample) | ✅ present on all 5 sampled |

## New Project Verification
No new project proposed (no PR/Issue event). Skipped.

## Maintainer Review Opinion
**RECOMMEND: Needs discussion (data-refresh action, not a code/PR block).** Confidence: **high** — based on deterministic `gh api` star comparisons across 28 repos and HTTP link checks.

Rationale: The list is structurally healthy (bilingual parity, no dupes, valid links, topic compliance), but the displayed star counts are materially out of date. The README footer claims "Star counts as of 2026-08-15" (13 days before this audit), yet live `gh api` data shows systematic, large upward drift on **every** sampled repo — e.g. `dsh-market/dsh-market` 156→2675 (+1615%), `Lum1104/dsh-browser` 114→500 (+339%), `omdsh-dev/DSH-better-sidebar` 911→3058 (+236%), `liustack/modlens` 1525→3734 (+145%), `deepseek-ai/deepseek-harness` 104.5k→201.6k (+93%). This undermines the list's credibility as a "curated, fresh" resource. I recommend a full star refresh (re-pull all 95 repos via `gh api`) and updating the "as of" date. No removal or re-categorization is warranted — all entries remain valid and on-topic.

Suggested comment body (for the tracking issue / discussion, friendly tone):
> 🤖 Curator audit (2026-08-28): list structure is solid — EN/ZH fully synced, no dupes, all links live, plugins carry `dsh-plugin`. One actionable item: **star counts are badly stale**. Sampled repos are up 8%–339% vs the "2026-08-15" snapshot (e.g. dsh-market 156→2675, DSH-better-sidebar 911→3058). Proposing a full star refresh + date bump. Happy to generate the patch — ping @hdjekuue if you'd like me to open it.

## Auto Labels
None — no PR/Issue event to label. (Auto-labeling only applies when `GH_PR`/`GH_ISSUE` is set.)

## Repo Health
- **Star drift (HEADLINE):** Severe and systematic. Sampled 28 repos, all grew since the listed "2026-08-15" snapshot. Representative drift:
  - `dsh-market/dsh-market`: 156 → 2675 (+1615%)
  - `Lum1104/dsh-browser`: 114 → 500 (+339%)
  - `csyangwen/dsh-memory-evolve`: 68 → 256 (+276%)
  - `omdsh-dev/DSH-better-sidebar`: 911 → 3058 (+236%)
  - `zhu1090093659/dsh-web-ui`: 2250 → 6362 (+183%)
  - `ccch1mneyyy/dsh-TUI`: 1047 → 2654 (+153%)
  - `deepseek-ai/deepseek-harness`: 104.5k → 201.6k (+93%)
  - `titanwings/colleague-skill`: 22228 → 24078 (+8%) — lowest drift
  - `QwenLM/Qwen-MM-Plugins`: 2549 → 2781 (+9%)
  - `zizaiwo/dsh_plugins`: 0 → 0 (stable)
- **Duplicates:** None detected (95 unique GitHub repos; repeated `deepseek-ai/deepseek-harness` / topic links are distinct link contexts, not duplicate table rows).
- **Broken links:** None. All GitHub / deepwiki / deepseekagent.io URLs returned HTTP 200. The two npm URLs returned 403 via `curl` (npm bot protection) but the npm registry API confirms both packages exist (`@deepseek-ai/dsh` v0.1.1-rc.2, `create-dsh-plugin` v0.2.0).
- **Topic compliance:** Sampled `dsh-market/dsh-market`, `liustack/modlens`, `ccch1mneyyy/dsh-TUI`, `omdsh-dev/dsh-at-file`, `zizaiwo/dsh_plugins` all carry the `dsh-plugin` topic. ✅

## Proposed Patches
None applied (draft generator only; a 95-row star refresh is large and should be human-approved). Recommend generating a refreshed `README.md` + `README.zh.md` star column via a scripted `gh api` pull, then bumping the "as of 2026-08-15" date to 2026-08-28.

## Next Steps
1. **Refresh stars** (recommended): run a script that re-pulls `stargazers_count` for all 95 repos and rewrites both README tables + the "as of" date. I can generate this patch on request.
2. Open a tracking issue or PR for the star refresh; optionally ping `@hdjekuue`.
3. No removals/re-categorizations needed at this time.

## Sources
- Local: `README.md`, `README.zh.md`, `.opencode/agent/curator.md` (read)
- `gh api repos/<owner>/<repo> --jq .stargazers_count` for 28 repos (star drift)
- `gh api repos/<owner>/<repo> --jq .topics` for 5 plugins (dsh-plugin topic)
- `curl -w "%{http_code}"` link checks: github.com, deepwiki.com, deepseekagent.io, npmjs.com (403 = bot protection)
- `curl https://registry.npmjs.org/<pkg>/latest` → confirmed `@deepseek-ai/dsh` 0.1.1-rc.2, `create-dsh-plugin` 0.2.0
