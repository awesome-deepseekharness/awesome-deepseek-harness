# Curator Report — 2026-09-04 07:12 UTC (model: opencode/muse-spark-1.3-contributor-free)
> Auto-generated by opencode headless (free-model traversal) — experimental, needs human review.

## Summary
No PR/Issue event — routine health audit only. Tables are structurally healthy (98 rows each, EN/ZH in sync, no duplicate projects, all sampled links return 200), but star counts are materially stale: all 8 sampled repos drifted upward, several by >100%, so a full star-refresh pass is due.

## Preliminary Checks
| Check | Result |
| --- | --- |
| Event type | No PR/Issue event — health audit only, nothing to triage |
| Title format | N/A (no PR) |
| Bilingual (README.md + README.zh.md) | ✅ Verified in sync — 98 table rows each, `diff` of extracted `github.com/owner/repo` sets is empty |
| Star accuracy | ❌ Stale — 8/8 sampled rows below live counts (see Repo Health) |
| dsh-plugin topic | N/A (no new project this run) |
| Files touched | None — read-only audit, no edits made |

## New Project Verification
No new project proposed in this event (no `GH_PR`/`GH_ISSUE` set). No verification performed.

## Maintainer Review Opinion
**RECOMMEND: Needs discussion (routine maintenance, not urgent); confidence: high.**

Rationale: the list itself is in good shape — zero project duplicates (only expected repeated header links like `deepseek-ai/deepseek-harness`, `topics/dsh-plugin`), EN/ZH parity confirmed via set-diff, and all 6 spot-checked URLs (4 GitHub repos + DeepWiki + deepseekagent.io guide) return HTTP 200. The one actionable finding is star drift: listings stamped "as of 2026-08-15" are now ~3 weeks old and every sampled row lags live counts (e.g., `anywhere-labs/deepseek-harness-desktop` 3,596 → 23,454; `NanmiCoder/dsh-agent-teams` 290 → 1,345). A maintainer-triggered star-refresh workflow run (or the periodic curator pass) would resolve it; no content removals or recategorizations are warranted from this audit.

Suggested comment body (only if opening a health issue — otherwise no action needed):
> 🤖 Curator health audit (2026-09-04): tables healthy — 98 rows EN/ZH in sync, no dupes, sampled links all 200. Star counts are stale (8/8 sampled rows below live, up to +552%). Suggest running the star-refresh workflow at convenience. cc @hdjekuue

## Auto Labels
None — no PR/Issue in this event, so no labels added or removed.

## Repo Health
- **Item counts:** 98 table rows in `README.md`, 98 in `README.zh.md` (includes Core Infrastructure + Learning rows); EN↔ZH project sets identical (`diff` empty). ✅
- **Duplicates:** none among listed projects. Repeated URLs are only intentional header/footer links (`awesome-deepseekharness/awesome-deepseek-harness`, `deepseek-official-tracker`, `cordiverse/cordis`, `deepseek-ai/deepseek-harness`, `topics/dsh-plugin`). ✅
- **Star drift (sampled 8 via `gh api repos/{owner}/{repo} --jq .stargazers_count`):** ALL stale (listed → live):
  - `liustack/modlens`: 1,525 → 3,857 (+153%)
  - `zhu1090093659/dsh-web-ui`: 2,250 → 6,813 (+203%)
  - `anywhere-labs/deepseek-harness-desktop`: 3,596 → 23,454 (+552%)
  - `ccch1mneyyy/dsh-TUI`: 1,047 → 2,821 (+169%)
  - `titanwings/colleague-skill`: 22,228 → 24,330 (+9%)
  - `junhoyeo/tokscale`: 4,963 → 5,288 (+7%)
  - `QwenLM/Qwen-MM-Plugins`: 2,549 → 2,801 (+10%)
  - `NanmiCoder/dsh-agent-teams`: 290 → 1,345 (+363%)
  - Stamp in README says "Star counts as of 2026-08-15" — refresh due.
- **Broken links (spot-check via `curl -L --max-time 15 -w "%{http_code}"`):** all 200 — `liustack/modlens`, `zhu1090093659/dsh-web-ui`, `anywhere-labs/deepseek-harness-desktop`, `titanwings/colleague-skill`, DeepWiki page, deepseekagent.io guide. ✅
- **Repo state:** clean `git status`, HEAD at `a7267b9`; `assets/star-history/` present (dark/light/svg + png). ✅

## Proposed Patches
None — read-only audit, no files edited.

## Next Steps
- Optional (maintainer): trigger the star-refresh workflow, e.g. `gh workflow run <star-refresh-workflow> --ref main` (check `.github/workflows/` for exact name), or wait for the scheduled curator pass.
- No `gh pr comment` / `gh pr create` needed — no PR/Issue under review.

## Sources
- Local files read: `README.md` (243 lines), `README.zh.md` (244 lines), `CONTRIBUTING.md`
- Local evidence: `grep` row counts + duplicate scan + EN/ZH set diff (`/tmp/en.txt` vs `/tmp/zh.txt`); `git log --oneline -8`; `git status --short`; `ls assets/star-history/`
- Live data via `gh api repos/{owner}/{repo} --jq .stargazers_count` for the 8 repos listed above
- Live link checks via `curl -s -o /dev/null -w "%{http_code}" -L` for the 6 URLs listed above
- No webfetch/websearch/kitesurf needed (no new project to verify, no JS-heavy pages encountered)
