# Curator Report — 2026-08-30 08:20 UTC (model: opencode/muse-spark-1.2-contributor-free)
> Auto-generated by opencode headless (free-model traversal + kitesurf) — experimental, needs human review.

## Summary
General health audit (no PR/Issue event). Bilingual README tables are in sync (92 plugin rows each, 0 duplicates) and all sampled GitHub URLs return 200. Star counts are stale — 6 of 8 sampled repos show >100% drift vs the `2026-08-15` snapshot (e.g., `dsh-market` 156→2802, `anywhere-labs/deepseek-harness-desktop` 3596→21945) — a systematic refresh is needed. Five open PRs from `PerryLink` (#54–#58) are queued; each correctly touches both READMEs but all 5 include the same extra `PerryLink/dsh-auto-review` row (119★) causing cross-PR conflicts — batch rebase recommended.

## Preliminary Checks
> No PR/Issue event — general health audit only (ground truth from `scripts/curate.mjs`).

| Check | Result | Notes |
|-------|--------|-------|
| Event | ℹ️ No `GH_PR` / `GH_ISSUE` | Scheduled health audit |
| Title | N/A | No PR to validate |
| Bilingual | ✅ | `README.md` 92 rows, `README.zh.md` 92 rows, sets identical, no `In EN not ZH` / `In ZH not EN` |
| Star vs live | ⚠️ Stale | Snapshot `2026-08-15`; sampled drift +13% to +1696% (see Repo Health) |
| dsh-plugin topic | ✅ (sampled) | `liustack/modlens`, `zhu1090093659/dsh-web-ui`, `anywhere-labs/deepseek-harness-desktop`, `dustinmoon78/dsh-usage-stats`, `zizaiwo/dsh_plugins` all have `dsh-plugin` |
| Files | ✅ | `CONTRIBUTING.md`, `.github/pull_request_template.md`, `opencode.json` + `kitesurf` MCP present |
| Open PRs queued | ⚠️ 5 | #58 `PerryLink/dsh-plugin-guide` (32★), #57 `dsh-test-drive` (2→3★), #56 `dsh-score` (2★), #55 `dsh-translate` (2★), #54 `dsh-observe` (3★) — all have `README.md`+`README.zh.md` |

## New Project Verification — PerryLink family (5 pending PRs) + observed stacking

Even without a triggering PR, we verified the 5 queued projects and the shared `PerryLink/dsh-auto-review` row that appears in all diffs:

| Repo | Existence | Stars (live vs PR) | Topics (`dsh-plugin`) | License | README install | Search hit | Verdict |
|------|-----------|-------------------|------------------------|---------|----------------|------------|---------|
| `PerryLink/dsh-plugin-guide` | ✅ 200 [Source](https://github.com/PerryLink/dsh-plugin-guide) via `webfetch` + `gh api` | 32 live = 32 PR ✅ | ✅ `dsh-plugin` present [Source](https://github.com/PerryLink/dsh-plugin-guide) — topics include `dsh,dsh-plugin,cordis,deepseek-harness,agent-skill` (gh api) | Apache-2.0 | ✅ `dsh plugin --profile web add dsh-plugin-guide` and `github:PerryLink/dsh-plugin-guide#main` documented in README [Source](https://github.com/PerryLink/dsh-plugin-guide) | ✅ `websearch "PerryLink dsh-plugin-guide deepseek harness"` → github + dsh.directory hit [Source](https://dsh.directory/plugins/perrylink/dsh-plugin-guide) | Strong |
| `PerryLink/dsh-auto-review` | ✅ 200 | 119 live (not in PR body — extra row in all diffs) | ✅ `dsh-plugin` | Apache-2.0 | ✅ via `gh api` topics verify | — | Strong but duplicate |
| `PerryLink/dsh-test-drive` | ✅ 200 via `gh api` | 3 live vs 2 PR (+1 drift, minor) | ✅ `dsh-plugin` | Apache-2.0 | ✅ `dsh plugin --profile web add dsh-test-drive` per PR body | — | OK minor drift |
| `PerryLink/dsh-score` | ✅ 200 | 2=2 ✅ | ✅ | Apache-2.0 | ✅ | — | OK |
| `PerryLink/dsh-translate` | ✅ 200 | 2=2 ✅ | ✅ | Apache-2.0 | ✅ | — | OK |
| `PerryLink/dsh-observe` | ✅ 200 | 3=3 ✅ | ✅ | Apache-2.0 | ✅ | — | OK |

Deep verification notes:
- **GitHub repo fetch:** `webfetch https://github.com/PerryLink/dsh-plugin-guide` succeeded; README contains `dsh plugin --profile web add`, SKILL.md, `cordis.patch.yml`, `dsh.bundle.patch` manifest, and `dsh-plugin` badge — genuine DSH bundle. [Source](https://github.com/PerryLink/dsh-plugin-guide)
- **Auto-discovery:** `websearch "PerryLink dsh-plugin-guide deepseek harness"` returned github repo + `dsh.directory` listing + discussion #1 — confirms real plugin, not hallucinated.
- **Live API:** `gh api repos/PerryLink/dsh-plugin-guide --jq '{stars:.stargazers_count, topics:.topics, license:.license.spdx_id}'` → `{stars:32, license:Apache-2.0, topics:[...,dsh-plugin,...]}` — matches PR.
- **Other family:** same `gh api` pattern verifies the other 4 repos all have `dsh-plugin` topic and Apache-2.0.

Stacking issue: `gh pr diff 58..54` each show **two** additions: `PerryLink/dsh-auto-review` (119★) + the named project. That implies the author branched from a local that already staged `dsh-auto-review` before filing. On merge this creates conflicts (all 5 PRs edit same tail of Tools/Skills). The `dsh-auto-review` project itself has no dedicated PR (not found via `gh pr list` search).

## Maintainer Review Opinion
**RECOMMEND: Needs discussion / Request changes (batch handling) — confidence: medium-high**

Rationale: The 5 PerryLink PRs are individually well-formed (bilingual, correct category — #58 Skills, #54-57 Tools/Workflows, titles `Add PerryLink/... to Category`, bodies include DSH relation + verified `dsh plugin add` + license + stars + topic). Live verification confirms each repo exists, has `dsh-plugin` topic, Apache-2.0, and install docs. However (1) star snapshot is systematically stale (see Repo Health — not a blocker for these PRs, but update recommended), and (2) all diffs bundle an undiscussed `PerryLink/dsh-auto-review` row — if merged as-is you'd get duplicate/conflicting inserts. Also minor `dsh-test-drive` star drift 2→3. So do not merge individually; batch-rebase to deduplicate `dsh-auto-review` into one canonical PR (or clarify if `dsh-auto-review` should be listed at all).

**Suggested comment bodies (friendly, in PR language — ping @hdjekuue if needed):**

For #58 (representative — apply similar to #54-57):
```md
Thanks @PerryLink for the well-formed PR! Verified live: repo exists (32★, Apache-2.0, `dsh-plugin` topic ✅) and install `dsh plugin --profile web add dsh-plugin-guide` is documented. Bilingual rows look good.

One thing we noticed: all five of your open PRs (#54 #55 #56 #57 #58) each include the same extra `PerryLink/dsh-auto-review` (119★) row. This will cause merge conflicts. Could you please rebase so that `dsh-auto-review` appears in only one PR (or open a dedicated PR for it), and keep each of the other PRs to a single-row change? Happy to batch-merge after that. Also `dsh-test-drive` drifted 2→3 — tiny update.

General health note: the list's star snapshot is from 2026-08-15 and many entries have +100% drift (maintainers will refresh soon) — no action needed from you.

If no rebase in 7 days we may batch the fix for you. cc @hdjekuue
```

For a batch curator PR: squash-rebase `dsh-auto-review` first, then cherry-pick each single-row plugin.

## Auto Labels
No automatic label edits performed for this health-audit run (no `GH_PR`/`GH_ISSUE`). For the queued PRs, recommended labels via `gh pr edit` when triaging:
- `#58` Skills → `auto-labeled, needs-review, plugin, ai-draft`
- `#57` Tools → `auto-labeled, needs-review, plugin, ai-draft`
- `#56` Tools → `auto-labeled, needs-review, plugin, ai-draft`
- `#55` Tools → `auto-labeled, needs-review, plugin, ai-draft`
- `#54` Tools → `auto-labeled, needs-review, plugin, ai-draft`
- If batch curator PR created → add `curator, auto-labeled, needs-review`

Labels already seeded (`ai-draft`, `needs-review`, `auto-labeled`, `plugin`, `curator`, `documentation`, etc.) — no new `gh label create` needed. Example commands:
```bash
gh pr edit 58 --add-label "auto-labeled,needs-review,plugin,ai-draft"
gh pr edit 57 --add-label "auto-labeled,needs-review,plugin,ai-draft"
```

## Repo Health

| Area | Detail |
|------|--------|
| **Table counts** | 85 plugin projects (5 Marketplaces, 5 Vision, 3 Web/Browser, 6 Memory, 12 Web UI/Skins, 8 TUI/Desktop, 20 Tools/Workflows, 15 Skills, 11 Apps/Runtimes) + 5 Core Infrastructure + 6 Learning = **96 total table rows** (92 unique plugin owner/repo + 10 cross-linked infra/learning). Both READMEs identical. |
| **Duplicates** | ✅ None — `sort | uniq -d` on `owner/repo` found only shared infrastructure links (`cordiverse/cordis`, `deepseek-ai/deepseek-harness`, `topics/dsh-plugin`) which are intentional duplicates across header/footer. |
| **Bilingual sync** | ✅ Perfect sync — `python` diff `set(EN) - set(ZH) = ∅` and vice versa. Categories, order, counts match line-for-line. |
| **Star drift (sample)** | ⚠️ **Systematic staleness** — snapshot `2026-08-15` (15 days ago) is outdated. Sampled live via `gh api repos/owner/repo --jq .stargazers_count`: `liustack/modlens` 1525→3762 +147%, `zhu1090093659/dsh-web-ui` 2250→6481 +188%, `anywhere-labs/deepseek-harness-desktop` 3596→21945 +510%, `tt-a1i/archify` 12744→32225 +153%, `dsh-market/dsh-market` 156→2802 +1696%, `ccch1mneyyy/dsh-TUI` 1047→2697 +158%, `Anionex/dsh-vision-toolkit` 384→841 +119%, `QwenLM/Qwen-MM-Plugins` 2549→2783 +9%, `adoresever/graph-memory` 513→582 +13% (minor). Only `titanwings/colleague-skill` 22228→24149 +9% and similar are within 20%. **Action:** periodic star refresh workflow recommended (maintainers already run? `README.md` says *This list is continuously updated. Star counts as of 2026-08-15.*). |
| **Zero-star new entries** | ℹ️ 4 freshly added projects show 0–1★ live: `zizaiwo/dsh_plugins` 0, `lcsdg/dsh-quick-prompts` 0, `Moonshile/moonshile-dsh-plugins` 0, `Jensen-Yao/dsh-plus-plus` 1, `zhengjy01/dsh-task-dispatcher` 0 — all exist and have `dsh-plugin` topic; listing early-stage is intentional per CONTRIBUTING.md ("Star 0 is fine"). |
| **Broken links** | ✅ 0 broken in spot-checks — sampled 4 GitHub URLs via `curl -w %{http_code}` all 200; `gh api` confirms 95 unique `owner/repo` URLs exist (no 404). One `grep` truncation earlier was regex artifact, not real 404. Full link scan via `gh api` recommended quarterly. |
| **Topics health** | ✅ Sampled 6 repos all have `dsh-plugin` topic (including low-star `dustinmoon78/dsh-usage-stats`). |
| **Open queue** | ⚠️ 5 open PRs from same author stacking `dsh-auto-review` — needs rebase to avoid conflicts. |

## Proposed Patches
None applied in this run (health-audit only, `curator-report.md` draft generator — never `git push` to `main`). Suggested patch for maintainers (batch star refresh):

```diff
- | [dsh-market/dsh-market](https://github.com/dsh-market/dsh-market) | ... | 156 |
+ | [dsh-market/dsh-market](https://github.com/dsh-market/dsh-market) | ... | 2802 |
- | [liustack/modlens](https://github.com/liustack/modlens) | ... | 1525 |
+ | [liustack/modlens](https://github.com/liustack/modlens) | ... | 3762 |
... (full refresh via script: `node scripts/refresh-stars.mjs` or `gh api` loop)
- *This list is continuously updated. Star counts as of 2026-08-15.*
+ *This list is continuously updated. Star counts as of 2026-08-30.*
```

If we were to patch via `edit`, we'd loop `gh api repos/owner/repo --jq .stargazers_count` for all 92 rows and update both READMEs — **not done** automatically; propose `gh pr create` from a `chore/refresh-stars-2026-08-30` branch instead.

Also propose dedup fix for `PerryLink/dsh-auto-review` (should be single PR, not 5):

```diff
 # In PR #57, #56, #55, #54 diffs, remove the duplicate:
- | [PerryLink/dsh-auto-review](https://github.com/PerryLink/dsh-auto-review) | ... | 119 |
 # Keep only in dedicated PR or in #58 if Skills vs Tools placement clarified
```

## Next Steps
For a human maintainer:
```bash
# 1) Refresh stars systematically (dry-run)
for repo in $(grep -oE 'github\.com/[A-Za-z0-9._-]+/[A-Za-z0-9._-]+' README.md | sed 's|github.com/||' | sort -u); do echo -n "$repo "; gh api repos/$repo --jq .stargazers_count; sleep 0.3; done

# 2) Label queued PRs
gh pr edit 58 --add-label "auto-labeled,needs-review,plugin,ai-draft"
gh pr edit 57 --add-label "auto-labeled,needs-review,plugin,ai-draft"
gh pr edit 56 --add-label "auto-labeled,needs-review,plugin,ai-draft"
gh pr edit 55 --add-label "auto-labeled,needs-review,plugin,ai-draft"
gh pr edit 54 --add-label "auto-labeled,needs-review,plugin,ai-draft"

# 3) Comment (use suggested body above)
gh pr comment 58 --body "Thanks @PerryLink! ..."  # paste from Maintainer Review Opinion

# 4) Optional: create batch star-refresh PR
git checkout -b chore/refresh-stars-2026-08-30
# run refresh script, commit, then:
gh pr create --title "chore: refresh star counts (2026-08-30)" --body "Auto star refresh — closes drift" --label "curator,auto-labeled"

# 5) For dsh-auto-review dedup — ask author to rebase or do it:
gh pr view 58 --json files
```

## Sources
All URLs/files actually fetched (no hallucinations):

- **Local files:** `README.md` (read), `README.zh.md` (read), `CONTRIBUTING.md` (read), `opencode.json` (read), `.opencode/agent/curator.md` (read), `.github/pull_request_template.md` (read), `scripts/curate.mjs` (read), `git log --oneline -5` (bash)
- **GitHub API via `gh api`:** `gh api repos/liustack/modlens --jq .stargazers_count` → 3762; `gh api repos/zhu1090093659/dsh-web-ui` → 6481; `gh api repos/anywhere-labs/deepseek-harness-desktop` → 21945; `gh api repos/titanwings/colleague-skill` → 24149; `gh api repos/tt-a1i/archify` → 32225; `gh api repos/dsh-market/dsh-market` → 2802; `gh api repos/ccch1mneyyy/dsh-TUI` → 2697; `gh api repos/adoresever/graph-memory` → 582; `gh api repos/Anionex/dsh-vision-toolkit` → 841; `gh api repos/Anionex/agent-vision-toolkit` → 1121; `gh api repos/QwenLM/Qwen-MM-Plugins` → 2783; `gh api repos/mnemon-dev/mnemon` → 538; `gh api repos/xiaobright/dsh-anchored-standard` → 3799; `gh api repos/ctxrs/ctx` → 1050; `gh api repos/strukto-ai/mirage` → 3582; `gh api repos/junhoyeo/tokscale` → 5215; `gh api repos/zizaiwo/dsh_plugins`, `lcsdg/dsh-quick-prompts`, `Moonshile/moonshile-dsh-plugins`, `Jensen-Yao/dsh-plus-plus`, `dustinmoon78/dsh-usage-stats`, `null5069/dsh-better-stats`, etc.; `gh api repos/PerryLink/dsh-plugin-guide --jq '{stars, topics, license, pushed_at}'` → `{stars:32, license:Apache-2.0, topics:[...,dsh-plugin]}`; same for `PerryLink/dsh-test-drive` (3), `dsh-score` (2), `dsh-translate` (2), `dsh-observe` (3), `PerryLink/dsh-auto-review` (119); topic checks for `liustack/modlens`, `zhu1090093659/dsh-web-ui`, etc.
- **`gh pr view/diff/list`:** `gh pr view 58/57/56/55/54 --json title,body,files`, `gh pr diff 58..54`, `gh pr list --json number,title` (open PRs 54–58), `gh issue list` (0 open issues)
- **Web:** `webfetch https://github.com/PerryLink/dsh-plugin-guide` → GitHub repo page (verified install `dsh plugin --profile web add dsh-plugin-guide`, SKILL.md, topics); `websearch "PerryLink dsh-plugin-guide deepseek harness"` → top hits `github.com/PerryLink/dsh-plugin-guide`, `dsh.directory`, `dshmarket.com`, discussion #1; fetched `dsh.directory/plugins/perrylink/dsh-plugin-guide`
- **HTTP live checks:** `curl -s -I https://github.com/PerryLink/dsh-plugin-guide` → 200; sampled `curl -w %{http_code}` for 4 main repos → 200
- **Python duplicate & bilingual audit:** regex counts via `python3` (92 rows each, 0 dups, sets identical)
