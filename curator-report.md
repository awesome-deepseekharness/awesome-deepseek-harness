# Curator Report — 2026-08-28 03:25 UTC (model: opencode/muse-spark-1.2-contributor-free)
> Auto-generated by opencode headless (free-model traversal + kitesurf) — experimental, needs human review.

## Summary — 2-3 sentences
General health audit (no PR/Issue event). READMEs parity is 92 vs 92 rows, no duplicate `owner/repo`, sampled links all 200, but **star drift is severe (>100% on 6/8 core samples since 2026-08-15)** — star-refresh workflow needed. Bilingual sync and table structure are healthy; 5 newly added `0-star` plugins verified live as `0`.

## Preliminary Checks — table: Title ✅/❌, Bilingual ✅/❌, Star ✅/❌ (live N vs PR N), dsh-plugin ✅/❌, Files ✅/❌

| Check | Result | Details |
|-------|--------|---------|
| Event type | ℹ️ Health audit | `GH_PR=` `GH_ISSUE=` — no PR/Issue (ground truth from `scripts/curate.mjs`: "No PR/Issue event — general health audit only") |
| Title | N/A | No PR to validate against `Add owner/repo to Category` |
| Bilingual | ✅ | `README.md` 92 project rows vs `README.zh.md` 92 rows; `diff` of extracted `github.com/*` lists shows only expected share-URL param differences (`hashtags`, `title` encoding) |
| Star | ⚠️ Drift | See Repo Health — 7/8 samples drift >20% (e.g., `liustack/modlens` 1525→3732 +145%, `zhu1090093659/dsh-web-ui` 2250→6305 +180%) |
| dsh-plugin topic | N/A | No new repo to check |
| Files | ✅ | No PR files to check |

No PR/Issue-triggered preliminary failures.

## New Project Verification — for owner/repo: existence, topics, stars, license, README install, search hits (with [Source](url) for each, via webfetch/websearch/kitesurf)

*No new project proposal in this run (no `GH_PR`/`GH_ISSUE`).* For reference, verification protocol that would run on a new `owner/repo`:
- `webfetch https://github.com/owner/repo` → check README mentions `dsh`/`deepseek-harness` + install `dsh plugin add` — fallback to `kitesurf` browser if JS shell [Source: curate flow].
- `websearch "owner/repo dsh-plugin"` → `webfetch` top hit [Source: curator.md workflow].
- `bash: gh api repos/owner/repo --jq '{stars: .stargazers_count, topics: .topics, license: .license.spdx_id, pushed_at}'` → live stars vs PR, `dsh-plugin` topic, license [Source: gh api].
- `bash: curl -s -o /dev/null -w "%{http_code}" https://github.com/owner/repo` → 200 check [Source: curl].

Five newest `0-star` listings were spot-checked as sanity (not new PRs, but recent additions):
- `zizaiwo/dsh_plugins` → live 0 via `gh api repos/zizaiwo/dsh_plugins` — link 200 via `curl -L` [Source: gh api + curl]
- `lcsdg/dsh-quick-prompts` → live 0, 200 [Source: gh api + curl]
- `Moonshile/moonshile-dsh-plugins` → live 0, 200 [Source: gh api + curl]
- `Jensen-Yao/dsh-plus-plus` → live 0, 200 [Source: gh api + curl]
- `dustinmoon78/dsh-usage-stats` → live 0, 200 [Source: gh api + curl]

## Maintainer Review Opinion — RECOMMEND: Approve / Request changes (missing ZH, star drift, wrong category) / Needs discussion; confidence low/medium/high; 1-paragraph rationale citing evidence; suggested comment body (friendly, in PR language, ping @hdjekuue if needed)

**RECOMMEND: No PR to review — health audit only; schedule star-refresh (Request changes to data freshness, not to code). Confidence: medium.**

Rationale: Repository structure is sound (bilingual parity 92/92, no duplicate owner/repo beyond intentional `deepseek-ai/deepseek-harness` references, all sampled GitHub URLs return 200). However the `⭐` column is stale (dated 2026-08-15 per README footer) with drift far above the 20% threshold on most high-traction plugins — e.g., `anywhere-labs/deepseek-harness-desktop` 3596→21240 (+491%), `dsh-market/dsh-market` 156→2623 (+1581%), `omdsh-dev/DSH-better-sidebar` 911→3029 (+232%) — suggesting a bulk refresh is overdue before next curation round. No action needed on open PR #20 (test/curator infrastructure only).

Suggested comment body (for health PR if maintainers open one):
```
Thanks for keeping the list fresh! 🙌

Health audit 2026-08-28:
- ✅ Bilingual parity 92/92, no duplicates, sampled links 200
- ⚠️ Stars stale since 2026-08-15 — many items >100% drift (e.g., modlens 1525→3732, dsh-web-ui 2250→6305, anywhere-labs/desktop 3596→21240). Recommend running the star-refresh workflow before next release.
- ℹ️ 5 newest 0-star plugins verified live 0 and reachable.

No blocker — ready to Approve once star-refresh lands. Ping @hdjekuue if you want a bulk patch PR.
```

## Auto Labels — labels added via `gh pr/issue edit` (e.g., auto-labeled, needs-review, plugin/ai-draft/curator) + any new labels created

None — no `GH_PR`/`GH_ISSUE` in this event, so no `gh pr edit` / `gh issue edit` executed (per guardrail: labels only on PR/Issue events). Open PR #20 (`test(curator): verify /curator trigger after fix`) was not labeled in this audit run; it retains its existing labels.

## Repo Health — star drift, broken links, duplicates (or "no health event")

- **Counts:** 92 project rows in both READMEs (Marketplaces 5, Vision 5, Web&Brow 3, Memory 6, Web UI 12, TUI 8, Tools 20, Skills 15, Apps 11, Core 3, Learning 4) [Source: `read README.md` + python category counter].
- **Duplicates:** none among `owner/repo` rows; only intentional duplicates are `deepseek-ai/deepseek-harness`, `cordiverse/cordis`, `awesome-deepseekharness/*`, `topics/dsh-plugin` discussion links — `grep | sort | uniq -d` clean [Source: `bash: grep -o 'github.com/[^)]*' | uniq -d`].
- **Bilingual:** parity ✅ — `diff` of extracted github URLs shows only share-URL query diffs [Source: `bash: diff <(grep ...) <(grep ...)`].
- **Broken links (sample 5):** `dsh-market/dsh-market` 200, `liustack/modlens` 200, `zizaiwo/dsh_plugins` 200, `lcsdg/dsh-quick-prompts` 200, `Moonshile/moonshile-dsh-plugins` 200 via `curl -s -o /dev/null -w "%{http_code}" -L` [Source: bash curl].
- **Star drift — sample 16 (live via `gh api repos/... --jq .stargazers_count` vs listed):**

| Repo | Listed | Live (2026-08-28) | Drift |
|------|--------|-------------------|-------|
| liustack/modlens | 1525 | 3732 | +144.7% ⚠️ |
| zhu1090093659/dsh-web-ui | 2250 | 6305 | +180.2% ⚠️ |
| anywhere-labs/deepseek-harness-desktop | 3596 | 21240 | +490.7% ⚠️ |
| titanWings/colleague-skill | 22228 | 24057 | +8.2% ✅ |
| tt-a1i/archify | 12744 | 23909 | +87.6% ⚠️ |
| cordiverse/cordis | 3607 | 7719 | +114.0% ⚠️ |
| ccch1mneyyy/dsh-TUI | 1047 | 2635 | +151.7% ⚠️ |
| dsh-market/dsh-market | 156 | 2623 | +1581% ⚠️ |
| bradeGithub/DSH-Plugins-Marketplace | 46 | 149 | +223.9% ⚠️ |
| Nagi-ovo/dsh-find-plugins | 78 | 169 | +116.7% ⚠️ |
| adoresever/graph-memory | 513 | 577 | +12.5% ✅ |
| mnemon-dev/mnemon | 443 | 531 | +19.8% ✅ |
| omdsh-dev/DSH-better-sidebar | 911 | 3029 | +232.5% ⚠️ |
| xiaobright/dsh-anchored-standard | 1336 | 3779 | +182.9% ⚠️ |
| NanmiCoder/dsh-agent-teams | 290 | 1131 | +290% ⚠️ |
| deepseek-ai/deepseek-harness | 104.5k (display) | 200566 | — parsing note: README shows `104.5k` text |

  Drift >20% flagged per audit rule; 13/16 above threshold. Footnote `Star counts as of 2026-08-15` explains lag [Source: `read README.md:238`].

- **Fresh 0-star cohort:** 5 rows list `0` and live is `0` — accurate, not drift [Source: `gh api`].
- **Open PRs:** #20 `test(curator): verify /curator trigger after fix` (OPEN, author hdjekuue) — infrastructure test only — not a plugin addition [Source: `gh pr list` + `gh pr view 20`].
- **Open Issues:** 0 [Source: `gh issue list`].

## Proposed Patches — unified diff preview if you edited README (or "none")

none — no `edit` applied (PR-safe: draft generator never pushes to `main`). Recommended patch is a bulk star-refresh (update `⭐` column + footer date `2026-08-15` → `2026-08-28`). Maintainers can run existing refresh workflow; curator does not auto-push.

If maintainers want, next step patch command:
```bash
gh workflow run star-refresh.yml  # or: node scripts/refresh-stars.mjs (if present)
# fallback manual: gh api repos/owner/repo --jq .stargazers_count per row
```

## Next Steps — `gh pr comment` / `gh pr create` commands for human

- Star refresh (recommended within 7 days):
  ```bash
  gh workflow list
  gh workflow run <star-refresh-workflow>  # bump 2026-08-15 → 2026-08-28
  ```
- If opening a bulk patch PR:
  ```bash
  git checkout -b chore/refresh-stars-2026-08-28
  # edit README.md + README.zh.md ⭐ columns + footer date
  git diff  # review
  gh pr create --title "chore: refresh stars 2026-08-28" --body "Bulk refresh sampled drift >100% — see curator-report.md" --label curator,auto-labeled
  ```
- For PR #20 (test): no action needed; close after `/curator` verification if desired:
  ```bash
  gh pr comment 20 --body "Curator health audit 2026-08-28: parity ✅, links ✅, stars need refresh. Test PR can be closed — @hdjekuue"
  ```

## Sources — all URLs/files you actually fetched (gh api, webfetch, websearch, kitesurf)

- Files: `read README.md` (241 lines), `read README.zh.md` (242 lines), `read CONTRIBUTING.md`, `read .github/pull_request_template.md`, `read opencode.json`, `read scripts/curate.mjs` [Source: local read]
- `bash: grep -o 'github.com/[^)]*' | sort | uniq -d` — duplicates check [Source: bash]
- `bash: grep -c '| \[.*\](https://github.com'` — 92 rows each README [Source: bash]
- `bash: gh api repos/<16 repos> --jq .stargazers_count` — live stars for 16 samples (modlens, dsh-web-ui, anywhere-labs/desktop, colleague-skill, archify, cordis, dsh-TUI, dsh-market, bradeGithub, Nagi-ovo, adoresever, mnemon-dev, omdsh-dev/better-sidebar, xiaobright/anchored, NanmiCoder/agent-teams, zizaiwo etc.) [Source: gh api]
- `bash: curl -s -o /dev/null -w "%{http_code}" -L https://github.com/<5 urls>` — link checks 200 [Source: curl]
- `bash: diff <(grep -o 'github.com/[^)]*' README.md) <(grep ... README.zh.md)` — bilingual parity [Source: bash]
- `bash: gh pr list --json number,title,state` + `gh pr view 20 --json title,body,files,author` [Source: gh]
- `bash: gh issue list --json number,title,state` — 0 issues [Source: gh]
- `bash: git log --oneline -8` [Source: git]
- `bash: date -u` [Source: bash]
- No `webfetch`/`websearch`/`kitesurf` needed this run (no new `owner/repo` to verify); protocol reserved for future PR/Issue events as per `curator.md`.

